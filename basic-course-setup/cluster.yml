nodes:
  - address: 3.90.3.118
    user: ec2-user
    role:
      - etcd
      - worker
      - controlplane

ssh_key_path: ~/.ssh/DevOps123.cer

cluster_name: experts

kubernetes_version: v1.19.8-rancher1-1
network:
  plugin: calico
dns:
  provider: coredns
